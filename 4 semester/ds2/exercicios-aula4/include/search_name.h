#include "register.h"
#include "search_id.h"

/// search the element by name and prints the info
void search_name(int n);

/// do the binary search comparing strings.
 int binary_search_name(struct index_name_file *array, int n, char *name);
